import { NotAuthorized } from "./NotAuthorized";
import { PageNotFound } from "./PageNotFound";

const ShellPages = {
  NotAuthorized,
  PageNotFound,
};

export default ShellPages;
export { NotAuthorized, PageNotFound };
