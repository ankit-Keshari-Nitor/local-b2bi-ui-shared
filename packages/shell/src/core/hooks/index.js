import { usePage } from './page';
import { useDatatable } from './data-table';
import { useForm } from './form';

const ShellHooks = {
  useDatatable,
  useForm,
  usePage
};

export default ShellHooks;

export {
  useDatatable,
  useForm,
  usePage
}
